\documentclass{standalone}

\usepackage{tikz}
\usepackage{graphicx}
\usetikzlibrary{positioning, calc}

\begin{document}	
	\newcommand{\picWidth}{100px}
	\newcommand{\picHeight}{375px}
	%\def \topOffset {-0.57};
	\newcommand{\topOffset}{-0.57}
	\newcommand{\leftOffset}{0.12}
	\newcommand{\plotWidth}{3.21}
	\newcommand{\plotHeight}{11.43}
	\newcommand{\margin}{0.13}
	
	\newcommand{\textsize}{\tiny}
	
	\begin{tikzpicture}[every node/.append style={text=black, font=\textsize}]
		\node[inner sep=0, anchor=north west] (leftright) at (0,0) {\includegraphics[width=\picWidth, height=\picHeight]{../Examples/TextPlot_2.pdf}};
		\node[inner sep=0, anchor=north west] (leftUpperBorder) at (\leftOffset,\topOffset) {1};
		\node[inner sep=0, anchor=north west] (rightUpperBorder) at (\leftOffset + \plotWidth,\topOffset) {1};
		\node[inner sep=0, anchor=north west] (leftLowerBorder) at (\leftOffset,\topOffset - \plotHeight) {1};
		\node[inner sep=0, anchor=north west] (rightLowerBorder) at (\leftOffset + \plotWidth,\topOffset - \plotHeight) {1};
		\node[inner sep=0, anchor=east, align=right] at (\leftOffset - \margin, \topOffset - \plotHeight / 20) {$root$};
		\node[inner sep=0, anchor=east, align=right] at (\leftOffset - \margin, \topOffset - \plotHeight / 20 * 3) {$NewRatio$};
		\node[inner sep=0, anchor=west, align=left] at (\leftOffset + \plotWidth + \margin, \topOffset - \plotHeight / 20) {$root$};
		\node[inner sep=0, anchor=west, align=left] at (\leftOffset + \plotWidth + \margin, \topOffset - \plotHeight / 20 * 3) {$NewRatio$};
	\end{tikzpicture}
\end{document}
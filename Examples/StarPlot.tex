\documentclass{standalone}

\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{positioning, calc}


\begin{document}
	\newcommand{\picHeight}{400px}
	\newcommand{\picWidth}{520px}

	\newcommand{\PI}{3.141592}

	\newcommand{\centerX}{9.22}
	\newcommand{\centerY}{-6.4}
	\newcommand{\radius}{3.1}
	\newcommand{\lineOffset}{0.7}
	\newcommand{\textsize}{\tiny}
	\newcommand{\offset}{0.2}

	\newcommand{\angleDiff}{360 / 10}
	\pgfmathsetmacro\ax{\centerX + (\radius + \lineOffset) * sin(\angleDiff * 0)}
	\pgfmathsetmacro\ay{\centerY + (\radius + \lineOffset) * cos(\angleDiff * 0) + \offset}
	\pgfmathsetmacro\bx{\centerX + (\radius + \lineOffset) * sin(\angleDiff * 1) + \offset}
	\pgfmathsetmacro\by{\centerY + (\radius + \lineOffset) * cos(\angleDiff * 1)}
	\pgfmathsetmacro\cx{\centerX + (\radius + \lineOffset) * sin(\angleDiff * 2) + \offset}
	\pgfmathsetmacro\cy{\centerY + (\radius + \lineOffset) * cos(\angleDiff * 2)}
	\pgfmathsetmacro\dx{\centerX + (\radius + \lineOffset) * sin(\angleDiff * 3) + \offset}
	\pgfmathsetmacro\dy{\centerY + (\radius + \lineOffset) * cos(\angleDiff * 3)}
	\pgfmathsetmacro\ex{\centerX + (\radius + \lineOffset) * sin(\angleDiff * 4) + \offset}
	\pgfmathsetmacro\ey{\centerY + (\radius + \lineOffset) * cos(\angleDiff * 4)}
	\pgfmathsetmacro\fx{\centerX + (\radius + \lineOffset) * sin(\angleDiff * 5)}
	\pgfmathsetmacro\fy{\centerY + (\radius + \lineOffset) * cos(\angleDiff * 5) - \offset}
	\pgfmathsetmacro\gx{\centerX + (\radius + \lineOffset) * sin(\angleDiff * 6) - \offset}
	\pgfmathsetmacro\gy{\centerY + (\radius + \lineOffset) * cos(\angleDiff * 6)}
	\pgfmathsetmacro\hx{\centerX + (\radius + \lineOffset) * sin(\angleDiff * 7) - \offset}
	\pgfmathsetmacro\hy{\centerY + (\radius + \lineOffset) * cos(\angleDiff * 7)}
	\pgfmathsetmacro\ix{\centerX + (\radius + \lineOffset) * sin(\angleDiff * 8) - \offset}
	\pgfmathsetmacro\iy{\centerY + (\radius + \lineOffset) * cos(\angleDiff * 8)}
	\pgfmathsetmacro\jx{\centerX + (\radius + \lineOffset) * sin(\angleDiff * 9) - \offset}
	\pgfmathsetmacro\jy{\centerY + (\radius + \lineOffset) * cos(\angleDiff * 9)}

	\begin{tikzpicture}
		\node[inner sep=0, anchor=north west] (pic) at (0,0) {\includegraphics[width=\picWidth, height=\picHeight]{StarPlot_1.pdf}};
		\node[inner sep=0, anchor=south] at (\ax, \ay) {$ root $};
		\node[inner sep=0, anchor=west, align=left] at (\bx, \by) {$ \phantom{\times}NewRatio$\\$\times UseAdaptiveSizePolicy $};
		\node[inner sep=0, anchor=west, align=left] at (\cx, \cy) {$ \phantom{\times}NewRatio$\\$\times \log(NewRatio) $};
		\node[inner sep=0, anchor=west, align=left] at (\dx, \dy) {$ \phantom{\times}NewRatio^{2}$\\$\times UseAdaptiveSizePolicy$\\$\times \log(SurvivorRatio) $};
		\node[inner sep=0, anchor=west, align=left] at (\ex, \ey) {$ DisableExplicitGC $};
		\node[inner sep=0, anchor=north] at (\fx, \fy) {$ NewRatio $};
		\node[inner sep=0, anchor=east, align=right] at (\gx, \gy) {$ NewRatio^{2}\times$\\$UseAdaptiveSizePolicy$\phantom{$\times$}$  $};
		\node[inner sep=0, anchor=east, align=right] at (\hx, \hy) {$ NewRatio\times$\\$\log(NewRatio)\times$\\$\log(MaxTenuringThreshold)$\phantom{$\times$}$  $};
		\node[inner sep=0, anchor=east, align=right] at (\ix, \iy) {$ UseAdaptiveSizePolicy $};
		\node[inner sep=0, anchor=east, align=right] at (\jx, \jy) {$ \log(MaxTenuringThreshold) $};
	\end{tikzpicture}
\end{document}
